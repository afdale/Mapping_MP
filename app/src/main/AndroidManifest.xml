<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agneshandayani.mapping_mp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FormFinish"></activity>
        <activity android:name=".FormConfirmation" />
        <activity android:name=".FormUpdateIstirahatKedua" />
        <activity android:name=".FormUpdateIstirahat" />
        <activity android:name=".FormSubmit" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarcodeScannerActivity" />
        <activity android:name=".Adapter" />
        <activity android:name=".Container" />
        <activity android:name=".FormUpdate" />
    </application>

</manifest>